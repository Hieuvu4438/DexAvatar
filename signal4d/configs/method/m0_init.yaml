schema_version: "1.0"
method_name: signal4d_m0_init
seed: 12345
window: {length: 64, stride: 32, context: 8}
uncertainty: {mode: constant, sigma_min: 0.02, sigma_max: 0.2}
change_point: {mode: disabled, gamma: 2.0, threshold: 0.7}
contact: {enabled: false}
factors:
  observation: 1.0
  temporal: 0.0
  contact: 0.0
  collision: 0.0
solver: {learning_rate: 0.001, max_steps: 1, patience: 1, grad_clip_norm: 10.0}
