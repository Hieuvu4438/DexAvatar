schema_version: "1.0"
method_name: signal4d_m0_smoke
seed: 12345
window: {length: 16, stride: 8, context: 2}
uncertainty: {mode: constant, sigma_min: 0.005, sigma_max: 0.2}
change_point: {mode: disabled, gamma: 2.0, threshold: 0.7}
contact: {enabled: false}
factors: {observation: 1.0, temporal: 0.0, contact: 0.0, collision: 0.0}
solver:
  learning_rate: 0.001
  max_steps: 3
  patience: 3
  grad_clip_norm: 10.0
