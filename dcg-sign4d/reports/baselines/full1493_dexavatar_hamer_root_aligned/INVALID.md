# Superseded aggregation

The side-specific values are valid, but the first combined hand value averaged
population-level left/right means. Use the corresponding `_v2` artifact, which
aggregates available hands per frame and then frame-to-clip.

