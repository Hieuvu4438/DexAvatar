base: rdp_r2_contact_recovery_v7_branch_ema.yaml
output_dir: outputs/phase3_training/rdp_r2_contact_recovery_v8_hand_body_contained_seed42
model:
  temporal_contact:
    observation_hand_body_only: true
training:
  max_steps: 6000
  validation_interval: 500
  checkpoint_interval: 500
  early_stop_patience: 6
