base: rdp_r2_contact_recovery_v4.yaml
output_dir: outputs/phase3_training/rdp_r2_contact_recovery_v4_smoke
training:
  max_steps: 2
  batch_size: 2
  gradient_accumulation: 1
  workers: 0
  validation_batch_size: 4
  validation_interval: 1
  checkpoint_interval: 1
  early_stop_patience: 2
